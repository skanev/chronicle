<%= turbo_frame_tag ingredient do %>
  <%= inline_form_with model: ingredient do |form| %>
    <%= form.collection_select :product_id, current_user.products, :id, :name %>
    <%= form.number_field :quantity %>
    <%= form.submit 'Save' %>
  <% end %>
<% end %>
